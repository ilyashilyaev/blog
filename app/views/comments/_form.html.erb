

    <%= form_with(model: [ @article, @article.comments.build ],data:{remote: true }, local: true, id: 'comments-form') do |form| %>

      <p>
        <%= form.label :body %><br>
       <%= form.text_area :body %>
     </p>
      <p>
       <%= form.submit id: 'create_comment'%>
     </p>
    <% end %>


